# Source base config:
source ~/shell-stuff/screenrc

# Open tabs:
screen -t "login1" 0 zsh
screen -t "root"   1 zsh -c "su -"
screen -t "mgmt1"  2 zsh -c "su -"

# Hardline/caption:
caption always "%{Gk} [Neptuno] %{kg} %-w %{kY} %n %t %{kg} %+w %= %{Gk} $LOGNAME @ %H"
