# Source base config:
source ~/.screenrc.lila

# Open tabs:
screen -t "shell" 0 bash

# Hardline/caption:
caption always "%{Mk} [%S] %{km}%{km} %-w %{mM}%{wM} %n %t %{Mm}%{km} %+w %= %{Mk} $LOGNAME @ %H"
